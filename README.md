# Livelo Partners Streamlit App

This app scrapes Livelo partner data, calculates points per real (adjusted for USD to BRL), and displays it in an interactive Streamlit table.

## How to run locally

```bash
pip install -r requirements.txt
streamlit run app.py
